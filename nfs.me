NFS的基本原则是“容许不同的客户端及服务端通过一组RPC分享相同的文件系统”，它是独立于操作系统，容许不同硬件及操作系统的系统共同进行文件的分享。

了解一下nfs服务运行在哪些端口上，它默认需要使用5个端口，其中有4个端口是动态的，所以如果服务器和客户端之间有iptables，就要先把这4个动态端口设置成静态的，然后加入进防火墙规则里

应用：实现多台机器的图片文件共享，共享session文件。
